# Executable program configurations

This directory stores the canonical cooling program and its parameter bounds.
Runtime prompts expose only the current executable program; the complete accepted
ledger remains internal and is replayed from the initial version.

Declarations use strict JSON. The canonical owner is
`canonical_cooling_program.json`.
