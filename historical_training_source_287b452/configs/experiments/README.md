# Experiment configurations

Experiment declarations own suite membership and the single registered factor
changed by each ablation. `runtime.json` freezes the request contract while
credentials and endpoints remain environment-owned. Paid and physical execution
always requires an explicit execution flag.
