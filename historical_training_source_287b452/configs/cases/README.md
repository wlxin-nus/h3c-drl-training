# Case profiles

Each strict JSON file is the sole owner of one BOPTEST case's point
mapping, fixed controls, occupancy semantics, physical conditioning protocol,
comfort constants, graph, and initial executable program. Production control
code consumes the validated profile and must not branch on testcase or profile
names.
